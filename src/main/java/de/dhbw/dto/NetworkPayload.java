package de.dhbw.dto;

/**
 * Marker interface for all network payload data transfer objects (DTOs).
 * <p>
 * All payload classes that are sent over the network should implement this interface.
 * It allows for type safety and consistent handling of payload objects in generic mappers
 * and message processing systems.
 * </p>
 */
public interface NetworkPayload {
}
