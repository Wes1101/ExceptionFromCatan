package de.dhbw.catanBoard.hexGrid;

public record IntTupel(int q, int r) {
    // Ein 'record' generiert automatisch:
// - private final Felder 'x' und 'y'
// - Konstruktor, Getter (x(), y()), equals(), hashCode(), toString()
}
